[pytest]
testpaths = tests
addopts = --tb=short -v
markers =
    unit: Pure unit tests with no external services required
    db: Tests that mock database access
    http: Tests that mock HTTP/network calls
    subprocess: Tests that mock subprocess calls
    iraf: Tests that require IRAF installed (skipped in CI)
